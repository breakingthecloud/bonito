# bonito-store v0.1.0 — image used by examples/docker-compose.yml.
FROM python:3.12-slim

WORKDIR /app

COPY bonito-store/pyproject.toml bonito-store/README.md ./
COPY bonito-store/src ./src

RUN pip install --no-cache-dir .

ENV BONITO_STORE_PORT=8000
EXPOSE 8000

CMD ["bonito-store", "serve"]