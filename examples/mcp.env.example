# bonito-mcp v0.1.0 — environment template
# For `bonito-mcp`. See docs/mcp.md.

# Base URL of the running bonito-api
BONITO_API_URL=http://localhost:8100

# REQUIRED — API key matching the API's BONITO_API_KEY
BONITO_API_KEY=change-me